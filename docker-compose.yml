services:
  app:
    container_name: mundo-3d-app
    build:
      context: .
      dockerfile: Dockerfile
    image: mundo-3d-app:latest
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    expose:
      - "3000"
    networks:
      - internal

  lp-mundo-3d:
    image: nginx:latest
    container_name: lp-mundo-3d
    restart: unless-stopped
    depends_on:
      - app
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    expose:
      - "80"
    networks:
      - internal
      - proxy

networks:
  internal:
    driver: bridge
  proxy:
    external: true
